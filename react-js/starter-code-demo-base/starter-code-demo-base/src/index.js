import "./style.css";

const root document.querySelector("#root");

const initTodos = [
    "Take out the trash",
    "Shop at Fresh Thyme",
    "Call Mom",
    "Mow Lawn",
    "Litter Boxes Cleanup",
];

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<h1>Hello, world!<h1>);
export const add2Nums = (num1, num2) => num1 + num2;

